# Custom-RISC-V-Assembler-Instruction-Simulator
Designed and implemented an assembler and simulator for a subset of the RV32I RISC-V instruction set. The assembler translates assembly programs into 32-bit machine code, and the simulator executes the instructions while producing register and memory execution traces.
